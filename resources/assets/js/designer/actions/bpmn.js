/* eslint-disable import/prefer-default-export */
export const bpmn = {
    bpmn: {
        shape: {
            update: x => x,
            create: x => x
        },
        participant: {
            update: x => x,
            create: x => x
        },
        flow: {
            update: x => x,
            create: x => x
        },
        toXML: x => x,
        save: x => x
    }
};
