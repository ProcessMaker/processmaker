/* eslint-disable import/prefer-default-export */
export const designer = {
    designer: {
        drag: {
            toolbar: {
                start: x => x,
                end: x => x
            },
            shape: {
                start: x => x,
                end: x => x
            }
        },
        zoom: {
            in: x => x,
            out: x => x,
            reset: x => x
        }
    }
};
