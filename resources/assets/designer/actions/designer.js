/* eslint-disable import/prefer-default-export */
export const designer = {
    designer: {
        drag: {
            end: x => x,
            start: x => x
        },
        zoom: {
            in: x => x,
            out: x => x,
            reset: x => x
        }
    }
}