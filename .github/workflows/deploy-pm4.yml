name: Build PM4
on:
  pull_request:
    types: [opened, reopened, synchronize, edited]
  workflow_dispatch:
  workflow_call:
jobs:
  run:
    name: Run PM4-workflow
    uses: processmaker/workflows/.github/workflows/deploy-pm4.yml@main
    secrets: inherit
